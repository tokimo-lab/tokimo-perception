{
  "name": "@tokiomo/python-ocr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "MODELS_DIR=../../data/ai-models uv run uvicorn app.main:app --host 0.0.0.0 --port 5679 --reload"
  }
}
