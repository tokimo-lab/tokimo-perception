{
  "name": "@tokiomo/python-ocr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "MODELS_DIR=../../data/ai-models PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True uv run $([ \"$(uname)\" = \"Linux\" ] && echo '--extra paddle' || true) uvicorn app.main:app --host 0.0.0.0 --port 5679 --reload"
  }
}
